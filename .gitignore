# Arvyn-Project/.gitignore 

# Python artifacts
__pycache__/
*.pyc
venv/

# Front-end dependencies and caches (for completeness)
node_modules/
dist/

# Playwright browser installations (large binaries)
playwright-browsers/

# Critical Security Exclusions (Zero-Commit Principle) 
.env
# LangGraph checkpoint files must NEVER be committed as they hold sensitive state
*.sqlite


